    <script>

    function itemCounter(data) {
      var field = this.field
      return data.map(function (row) {
        return +row[field]
      }).reduce(function (sum, i) {
        return sum + i
      }, 0)
    }

    function sizeSummer(data) {
      var field = this.field
      return sizeFormatter(data.map(function (row) {
        return +row[field]
      }).reduce(function (sum, i) {
        return sum + i
      }, 0))
    }

    function dateFormatter(value, row, index) {
      var date = value.replace('T', ' ');
      return date.slice(0, 16);
    }

    function sizeFormatter(bytes, row, index, si=false, dp=2) {
      const thresh = 1024;

      if (Math.abs(bytes) < thresh) {
        return bytes + ' B';
      }

      const units = ['KiB', 'MiB', 'GiB', 'TiB', 'PiB', 'EiB', 'ZiB', 'YiB'];
      let u = -1;
      const r = 10**dp;

      do {
        bytes /= thresh;
        ++u;
      } while (Math.round(Math.abs(bytes) * r) / r >= thresh && u < units.length - 1);


      return bytes.toFixed(dp) + ' ' + units[u];
    }
  </script>
<!-- Load the JavaScript bulk last. Served locally; see partials/head for why. -->
  <script src="/js/vendor/jquery.min.js"></script>
  <!--
    Bootstrap's bundle, which embeds Popper. Popper was previously loaded as a separate
    script and used by nothing: it is only needed for dropdowns, tooltips and popovers, and
    this app has none. Using the bundle drops that separate request while still leaving
    Popper available, so the first dropdown anyone adds does not fail with an obscure error.
  -->
  <script src="/js/vendor/bootstrap.bundle.min.js"></script>
  <script src="/js/vendor/bootstrap-table.min.js"></script>
  <script>
    $(document).ready(function() {
      $('li.active').removeClass('active');
      $('a#{{ menuId }}').closest('li').addClass('active'); 
    });
  </script>
